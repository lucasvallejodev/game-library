{
  "name": "@game-library/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fastify HTTP API — the only process that touches the database",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@game-library/db": "workspace:*",
    "@game-library/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
