/**
 * Placeholder so the workspace package typechecks before Next.js lands.
 *
 * Increment 10 replaces this entirely: the App Router tree under src/app/,
 * the SCSS token layer under src/styles/, Radix-backed primitives under
 * src/components/ui/, and the typed API client. This tsconfig will switch to
 * a Next-specific base at the same time.
 *
 * Styling rules are non-negotiable and documented in
 * docs/frontend-guidelines.md: SCSS Modules with BEM, no utility-class
 * framework, raw colors only in _tokens.scss.
 */

export const WEB_PACKAGE_NAME = '@game-library/web'
